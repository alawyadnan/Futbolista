# Futbolista — تقرير الجودة

## نطاق الإصدار العام — 8 سبتمبر 2026

النشر الموافق عليه للتحسينات العامة فقط. الحسابات والتصويت معطّلان عبر `COMMUNITY_ENABLED=false`، ولا يشمل الإصدار نشر أي قواعد أو فهارس Firebase. أُعيد تشغيل الاختبارات النقية قبل تجهيز النشر: 113/113 ناجحة، دون كتابة بيانات. المراجعة التالية سجلّ الفحص المحلي السابق، بما فيه حدود ما لم يُختبر.

## آخر مراجعة: Community 500400 — 7 سبتمبر 2026

- 113/113 اختبار Node ناجح في الجولة الأخيرة؛ لا كتابة Firebase ولا اتصال شبكة أثناء الاختبارات. تشمل المحرك القديم، الإضافات الإحصائية، الثبات بعد تغيير الاسم، منع تكرار المرشحين، النقاط والتعادل، الحد الدقيق لـ24 ساعة، المسودات، جدولة الرسم، الروابط والكاش.
- فحص `node --check` لجميع ملفات JS وملفات اختبارات القواعد وسكربتات MJS ناجح، و`git diff --check` نظيف. فحص صياغة ملف اختبار القواعد لا يعني تشغيل قواعد Firestore.
- 26 حالة قواعد مجهزة للإصدار الحالي **لم تُشغّل** احترامًا لرفض كتابة بيانات حتى في المحاكي. لا اعتماد نهائي لأمن الميزات الجديدة قبل تنفيذ التحقق المناسب بتفويض منفصل.
- فُحصت Dashboard وLeaderboard وTable وPlayers وPlayer Profile والمقارنة المدمجة وHistory، بالعربية والإنجليزية، على 320 و360 و390 و430 و1440 بكسل. البيانات الحقيقية ظهرت: 26 مباراة و62 لاعبًا؛ فُحص أيضًا مصطفى (24 مشاركة و63 هدفًا) ومقارنته بصفوان. القراءة فقط، ولا ضغط حفظ/إضافة/تصويت.
- في مرور آلي من 70 حالة، كانت 3 مشاهدات مباشرة بعد تغيير المقاس على شاشة سابقة/مجاورة؛ أُعيد فحصها منفصلة بعد استقرار التخطيط والتأكد من الشاشة المطلوبة ونجحت. لم يُعامل عرض شاشة غير مطلوبة كنجاح.
- عرض المستند لم يتجاوز عرض الشاشة في الحالات المفحوصة؛ تمرير الجدول داخل حاويته. عناصر الإدارة الظاهرة للعامة: صفر. لا أسهم Leaderboard.
- سجل المتصفح في تبويب الفحص النظيف خالٍ من أخطاء JavaScript والتحذيرات. فُحص ظهور الفريقين والنتيجة في Safari على macOS بعد إزالة حركة شفافية التفاصيل، وحُفظت لقطة واضحة.
- في مراجعات محلية سابقة فُحصت واجهات الحساب المرتبط والطلب المعلق والمرفوض، وحقول التصويت ونتيجته المغلقة، ومسودة الاسم والاختيارات عند تغيير اللغة، وإظهار الإدارة بعد الدخول المحلي وإخفاؤها بعد الخروج. لم تُعد هذه الجولة تسجيل الدخول أو إرسال أي نموذج أو تجهيز المحاكي.
- التوقيت الجديد: أول سجل لمباراة جديدة ووثيقة التصويت يُحفظان في معاملة واحدة ووقت خادم؛ تنتهي المهلة بعد 24 ساعة دون تمديد بإضافة اللاعبين. لا فتح رجعي للمباريات التاريخية. أُزيل زر الفتح اليدوي وترجماته غير المستخدمة.
- تحديث الموارد 500400؛ إعداد Firebase و`data-engine.js` و`CNAME` لم تتغير. تصدير البيانات الخام كما هو. لا كتابة/حذف/ترحيل في الإنتاج ولا دفع GitHub أو نشر لهذه الدفعة.

### ما لم يمكن إثباته في هذه الجولة

- تطبيق قواعد Firestore الحالية وتنفيذ الحفظ الذري فعليًا: يتطلبان اختبارات كتابة محلية رفضها المستخدم.
- نجاح دخول إدارة الإنتاج، إرسال بريد تحقق/استعادة كلمة المرور، حساب جديد أو موافقة أو صوت فعلي: لم تُنفذ هذه العمليات.
- تطبيق PWA مثبت على iPhone حقيقي، لوحة مفاتيح iOS فعلية، وتعطيل الشبكة من الجهاز: لم يُختبر. اختبار المقاسات ليس بديلًا لجهاز حقيقي.
- لا نشر لقواعد الإنتاج ولا تفعيل للحسابات على الموقع؛ `COMMUNITY_ENABLED=false` حتى استكمال فحص الحماية وموافقة النشر.

التفاصيل، مسارات البيانات، خصوصية الأصوات بعد الإغلاق، وخطة الإطلاق في [COMMUNITY-RELEASE.md](./COMMUNITY-RELEASE.md).

---

## سجل مراجعة الإصدار السابق 500300

المحتوى التالي محفوظ كتقرير تاريخي؛ أرقامه وحدوده تخص ذلك الإصدار، وليست نتائج الجولة الحالية.

## البنية

- تطبيق صفحة واحدة متوافق مع GitHub Pages و`ftbll.live`، مبني بـHTML وCSS وJavaScript دون إطار عمل.
- `app.js` مسؤول عن Firebase Authentication والقراءة اللحظية من Firestore وحالة الواجهة والرسم والتنقل.
- `data-engine.js` محرك إحصائي نقي ومستقل يدعم السجلات الحالية والقديمة ويمنع تضخيم الإحصائيات بسبب الإدخالات المكررة.
- `i18n.js` يحتوي واجهتي العربية والإنجليزية وصيغ العد، و`ux-utils.js` يحتوي منطق UX النقي القابل للاختبار.
- `sw.js` و`manifest.json` وأيقونات PNG توفّر غلاف PWA قابلًا للتثبيت.

## التحسينات المنفذة

- واجهة رياضية داكنة ومتجاوبة بالكامل مع العربية وRTL وحالات التركيز وتقليل الحركة.
- لوحة رئيسية مركزة تعرض المباريات واللاعبين والفورم ولاعب الشهر والهدافين والسلاسل ونسبة الفوز.
- روابط قابلة للرجوع والمشاركة لكل قسم ولكل ملف لاعب، مع منع الروابط العامة من فتح شاشات الإدارة.
- روابط مشاركة نظيفة لملف اللاعب ولمقارنة لاعبين محددين، مع استرجاع طرفي المقارنة عند فتح الرابط مباشرة.
- فتح ملفات اللاعبين مباشرة من لوحة الصدارة والجدول والسجل والمقارنة وقائمة الزملاء.
- ملف لاعب محسّن يتضمن حصيلة فوز/تعادل/خسارة مرئية وزري مقارنة ومشاركة، ويرجع إلى القسم وموضع التمرير اللذين فُتح منهما.
- سجل مباريات قابل للبحث والتصفية والطي، ويعرض عشر مباريات في البداية ثم يحمّل المزيد عند الطلب لتقليل عناصر DOM على الجوال.
- فتح تفاصيل مباراة واحدة أو إغلاقها يحدّث عناصرها فقط ويحافظ على تركيز لوحة المفاتيح بدل إعادة رسم السجل كاملًا.
- إبراز عمود أو مقياس الترتيب النشط بصريًا ودلاليًا من دون إعادة أسهم Leaderboard.
- ترتيب مباريات اليوم نفسه حسب وقت إنشائها قبل حساب الفورم والسلسلة الحالية.
- تحقق أقوى من تاريخ المباراة وتوحيد مسافات وأحرف أسماء اللاعبين قبل فحص التكرار في الإدخالات الجديدة.
- منع حفظ نتيجة قبل اكتمال التحميل أو بلاعب لم يعد موجودًا، وإعادة حقلي الأهداف ونوع الهدف إلى قيم آمنة بعد الحفظ الناجح.
- تحسين تثبيت PWA على iPhone وAndroid ومراعاة المساحة الآمنة حول النتوء العلوي.
- تحميل اتجاه اللغة مبكرًا لمنع وميض LTR، وتحسين أسماء الوصول والعناوين وبنية المقارنة وعزل أسماء اللاعبين ثنائية الاتجاه.
- نوافذ آمنة ومتاحة لدخول الإدارة وإعادة الضبط، ورسائل تحميل ونجاح وخطأ واضحة.
- تحديث رقم موارد الواجهة إلى `500300` وإصلاح استراتيجية Service Worker لتخزين الاستجابات الناجحة فقط وانتظار الكتابة وحذف كاش Futbolista القديم فقط.

## سلامة البيانات والتوافق

- لم تتغير إعدادات Firebase أو Authentication أو أسماء مجموعتي `players` و`logs` أو أسماء الحقول.
- بقي `admin@ftbll.live` حساب الإدارة الوحيد، وبقيت Admin وEntry وSettings مخفية عن المستخدم العام.
- بقي تصدير JSON مبنيًا على بيانات Firestore الخام كما هو.
- لم تُنفذ أي كتابة أو حذف أو migration أو reset أثناء التطوير والاختبار.
- لم تُستخدم بيانات تجريبية بدل بيانات الإنتاج، وكانت مراجعة الإنتاج للقراءة فقط.
- راجعت القراءة الخام لبيانات الإنتاج: 62 لاعبًا و476 سجلًا. توجد 8 وثائق قديمة ناقصة لا تدخل في الحسابات، و89 سجلًا شرعيًا بصيغة `win` القديمة، ولا توجد تواريخ غير صالحة أو سجلات مرتبطة بلاعب محذوف أو أنواع مختلطة لبيانات المشاركة. توجد مجموعة تكرار واحدة من النوع نفسه يعالجها المحرك الحالي من دون تضخيم النتائج.
- لم تُضمّن قواعد Firestore في المستودع، لذلك لا يمكن إثبات صلاحيات الإنتاج من الكود المحلي وحده؛ لم تُغيَّر القواعد أو إعدادات Firebase.

## الاختبارات

- 41/41 اختبار Node ناجح لمحرك الإحصائيات ومنطق UX والترجمة والتنقل والمشاركة والتقسيم إلى صفحات والتحقق من مدخلات الإدارة ونسخة أصول PWA.
- فُحصت Dashboard وLeaderboard وTable وPlayers وPlayer Profile وCompare وHistory على عروض 320 و360 و390 و430 و1280 بكسل.
- لا يوجد تمرير أفقي للصفحة أو شريط الجوال؛ يظل تمرير الجدول داخليًا عند الحاجة.
- فُحصت العربية وRTL والإنجليزية، والروابط المباشرة، والرجوع، وروابط اللاعبين والمقارنة، وعناوين المتصفح، وتحميل المزيد في السجل.
- فُحصت دلالة ترتيب الجدول، وتمييز مقياس Leaderboard النشط، وعدم وجود أسهم Leaderboard، وحفظ تركيز لوحة المفاتيح عند طي تفاصيل المباراة.
- فُحص إخفاء عناصر الإدارة للعامة ونافذة الدخول وإغلاقها وإعادة التركيز، دون إرسال بيانات دخول.
- فُحصت وحدة التحكم في تبويب نظيف دون أخطاء JavaScript.
- لم يُختبر نجاح تسجيل الدخول الحقيقي لعدم توفر كلمة المرور، ولم يُضغط Add player أو Save entry أو زر التأكيد النهائي لإعادة الضبط، ولم يُجرَّب وضع انقطاع الشبكة فعليًا داخل المتصفح.
