# Futbolista — تقرير الجودة

## أحدث مراجعة — 500406، 11 سبتمبر 2026 (فحص ما قبل النشر)

أصبح ترتيب اللاعبين أكثر اختصارًا، مع إبراز الإحصائية المختارة وإتاحة بقية
الأرقام بزر «تفاصيل الأرقام» دون حذفها. تحسنت بطاقة MOTM والبروفايل والترندات
وقراءة الجداول والمقارنة، مع الحفاظ على الهوية الخضراء الداكنة والعربية والإنجليزية.
نجح **203 اختبارًا نقيًا/تكامليًا و36 اختبار حماية محليًا: 239 اختبارًا**.
نجحت **121 حالة عرض** تشمل اللغتين وعروض 320/360/390/430/1440 بكسل، وفتح تفاصيل
البروفايل صراحة في كل مقاس، وجميع خيارات ترتيب اللاعبين الأحد عشر. لا تجاوز
لعرض الصفحة أو قص في العناصر المفحوصة؛ بقيت الإدارة مخفية للعامة وسجل أخطاء
المتصفح فارغًا. اختُبر زر التفاصيل بلوحة المفاتيح وبقيت حالته بعد تبديل اللغة.
نجح فحص صياغة 35 ملف JavaScript و`git diff --check`.

الصور والواجهات استخدمت البيانات الحقيقية للقراءة فقط. لا كتابة أو حذف في
Firestore الإنتاج، ولا تغيير حسابات أو قواعد أو إعداد Firebase أو منطق الإحصائيات
والتصويت. اختبارات الكتابة اقتصرت على المحاكي المحلي المصرّح به وأُوقف بعدها.
لم يُعد دخول المدير تفاعليًا في هذه الجولة؛ الحماية وحالاته مغطاة بالاختبارات
الآلية القائمة. الهاتف الفعلي ودورة تحديث PWA دون اتصال غير مختبرين.
لم تتضمن مرحلة الفحص نشرًا؛ وافق المستخدم بعدها على نشر ملفات الواجهة فقط.
النسخة المنشورة السابقة 500405 محفوظة في `a7f9e01`.
راجع `RELEASE-500406.md` للتفاصيل وحدود التحقق؛ المراجعات التالية سجل تاريخي.

## أحدث مراجعة — 500405، 9 سبتمبر 2026 (فحص ما قبل النشر)

أزيل شرح توزيع نقاط التصويت من الرئيسية والبروفايل. تحسن ترتيب محتوى الرئيسية،
وأضيفت أشرطة نسبية للمتصدرين وتفاصيل تصويت قابلة للفتح، وأُصلحت مراكز أصحاب
الرصيد صفر دون تغيير الإحصائيات. نجح 197 اختبارًا نقيًا/تكامليًا و36 اختبار
حماية في المحاكي المحلي: **233 اختبارًا**. نجحت 110 حالات عرض عربية/إنجليزية
على 320/360/390/430/1440 بكسل؛ لا تجاوز عرض أو قص في البطاقات المفحوصة.
اختُبرت لوحة المفاتيح ودخول مدير المحاكي وخروجه، وبقي سجل أخطاء المتصفح فارغًا.
لم تتضمن مرحلة الفحص كتابة بيانات إنتاج أو تعديل قواعد أو إعداد Firebase أو نشر.
وافق المستخدم بعد اكتمال الفحص على نشر ملفات الواجهة فقط. راجع
`RELEASE-500405.md` للتفاصيل والحدود. النسخة السابقة 500404 نُشرت بالفعل في
الالتزام `3e913b3`؛ أوصاف «غير منشور» في المراجعات أدناه سجل تاريخي لمرحلة الفحص.

## آخر مراجعة — 500402، 9 سبتمبر 2026 (محلي، غير منشور)

- أضيف سجل كامل للاعب من ملفه، برابط ثابت يعتمد على `playerId`، مع فوز/تعادل/خسارة وأهداف للفترة المختارة. لا تطابق بالاسم ولا تضخيم بسبب تكرار السجلات؛ الأهداف العكسية منفصلة. الإجماليات تشمل كل النتائج المطابقة، وليس الصفحة الظاهرة فقط.
- تحسنت خطوات تأكيد البريد واختيار اللاعب وانتظار المدير، مع تنبيه Spam، وإظهار كلمة المرور، ورسائل خطأ قابلة للتصرف. تعديل الطلب المعلّق يظهر عند طلبه ويمكن إلغاؤه دون حفظ. رفض الربط لا يحذف الحساب.
- أسماء الفريقين فوق النتيجة وترتيب أ/ب ثابت ومعنون لقارئ الشاشة. أزيل تخفيض شفافية الفريق الخاسر، وكُبّرت تواريخ المباريات وبعض النصوص المهمة، وأُصلح التفاف الاسم والنتيجة على 320px.
- نجح **141/141** اختبار Node و**30/30** اختبار Firestore في المحاكي المحلي `demo-futbolista`، بإجمالي **171**. اختبارات القواعد تستخدم موافقة الاختبار المحلية السابقة؛ لا كتابة إنتاج. أضيف 23 اختبارًا: ملخص السجل والروابط، الهوية مع تشابه الأسماء، الفصل بين الأهداف العادية والعكسية، حالات الحساب، وتشغيل renderer الحساب الفعلي بمهايئات DOM/Firebase خاملة بلا شبكة أو حسابات اختبار جديدة.
- نجحت **101 مشاهدة تحقق**: العربية والإنجليزية على 320/360/390/430/1440 بكسل لـDashboard وLeaderboard وTable وPlayers والملف والمقارنة والحساب/التسجيل وسجل اللاعب والسجل العام. جرى التأكد من العرض الفعلي لكل مشاهدة؛ القراءات أثناء انتقال المقاس لم تُحتسب. لا تجاوز لعرض المستند ولا قص داخلي للنتيجة/البطاقات المفحوصة؛ تمرير الجدول يبقى داخليًا. الإدارة ظاهرة للعامة: صفر.
- مصطفى: السجل الكامل 24 مشاركة، 9 فوز، 5 تعادل، 10 خسائر، 63 هدفًا. فلتر أغسطس يعرض مباراتين، فوزًا وخسارة وهدفين. اختُبر إعادة تحميل الرابط المباشر، البحث بلا نتائج، مسح الفلاتر، طي التفاصيل وتحميل 20 مباراة من 26 دون تغيير الإجمالي.
- أُظهر وأُخفي نص كلمة مرور وهمي في نموذج الدخول دون إرساله؛ تغيير وضع الدخول يمسح المسودة ويعيد الإخفاء. تبويب فحص نظيف بلا أخطاء JavaScript أو تحذيرات؛ سجل التبويب القديم يحتوي تحذيرات اتصال قديمة قبل بدء الجولة. فحص الصياغة و`git diff --check` ناجحان.
- لا تغيير في `data-engine.js` أو قواعد Firestore أو إعداد Firebase أو المجموعات/الحقول أو حساب الإدارة أو تصدير JSON. لا تغيير لمهلة التصويت أو الحضور أو منع التصويت للنفس. الموارد والكاش 500402، مع إضافة `account-ux.js` إلى precache.
- لم يُجرَ دخول أو إنشاء حساب أو إرسال بريد تحقق/استعادة أو موافقة أو حفظ أو تصويت في الإنتاج. حالات التحقق/الطلب المعلق/المرفوض/المرتبط والإدارة اختُبرت آليًا بمهايئات خاملة، لا بجلسات إنتاج جديدة. لم يُختبر على هاتف مادي أو كتطبيق مثبت، ولا يُعد فحص المقاسات بديلًا لذلك.
- `main` في GitHub ما زال `a86868b10a99d679e5df0653367754ade84ff22e` وقت الفحص. لا commit أو push أو نشر لهذه الدفعة؛ النسخة المنشورة السابقة 500401 محفوظة.

## حالة النشر السابقة — 500401

نُشرت قواعد الإنتاج بعد التأكيد الصريح في 8 سبتمبر 2026، وطُوبقت نسخة المحرر كاملة بالملف المختبر قبل النشر. تحقق REST للقراءة فقط أثبت استمرار القراءة العامة لـ62 لاعبًا و476 سجلًا، ورفض قراءة `users` و`accountRequests` و`playerClaims` العامة بـ403. مجموعتا الملفات العامة والتصويت فارغتان وقابلتان للقراءة؛ لم تُنشأ أي بيانات اختبار في الإنتاج. أُفعّلت بوابة المجتمع محليًا بعد هذا التحقق، وأُعيد تشغيل 118 اختبارًا نقيًا و30 اختبار قواعد بنجاح.

نُشرت الواجهة والموارد وService Worker 500401 على https://ftbll.live/ بنجاح؛ عملية Pages رقم `34251218109` للإصدار `79f6a81` مكتملة بحالة success. ظهر زر الحساب ونموذج الدخول والإحصائيات الحقيقية على الموقع. تعذّر فتح سجل نسخ Firebase بعد إعادة تحميل اللوحة، لذا تحقق نشر القواعد مبني على مطابقة المحرر واختفاء حالة التعديلات غير المنشورة ثم فحص قواعد الوصول مباشرة، وليس على رقم نسخة جديد معروض في اللوحة.

فحص Firestore بعد النشر قرأ جميع صفحات النتائج: 62 لاعبًا و476 سجلًا، وآخر تعديل للاعبين 1 سبتمبر وللسجلات 2 سبتمبر. عدد الوثائق المعدلة يوم النشر (8 سبتمبر) صفر في المجموعتين؛ لم تُكتب أو تُحذف أو تُرحّل أي بيانات إنتاج.

فُحصت على الموقع المنشور Dashboard وLeaderboard وTable وPlayers وHistory والملف الشخصي والمقارنة (Mustafa: 24 مشاركة/63 هدفًا، Safwan: 23/50) وصفحة الحساب. عرضا الحساب والمقارنة اجتازا 320/360/390/430/1440 بكسل دون تمرير أفقي؛ بقيت الإدارة مخفية للعامة. فُحصت الإنجليزية أيضًا في صفحة الحساب بعرض 320 بكسل. تبويب إنتاج نظيف لم يسجل أخطاء JavaScript أو تحذيرات؛ احتوى التبويب القديم على تحذيرات اتصال Firebase تعود إلى ساعات سابقة، لذلك لم تُنسب إلى الإصدار الجديد. طُوبقت ملفات الإنتاج الأساسية العشرة حرفيًا مع الملفات المحلية المختبرة. لم تُرسل نماذج تسجيل أو حفظ أو تصويت إلى الإنتاج.

## مراجعة شرط حضور التمرين — 500401، 8 سبتمبر 2026

- نجح 118/118 اختبارًا نقيًا و30/30 اختبار قواعد محليًا. أضيف منع إنشاء/تعديل صوت لغير حاضر، عزل صلاحية كل تاريخ، أهلية المشارك المضاف لاحقًا، استبعاد أوراق غير الحاضرين من النتائج، واختبارات اختيار الجلسة وتبديل المراكز.
- تجربة الواجهة الفعلية في `demo-futbolista`: حساب QA مرتبط شارك في 11 أغسطس ولم يحضر 13 أغسطس. فُتح له تمرين 11 تلقائيًا؛ اختيار 13 أخفى نموذج التصويت وعرض السبب. العودة إلى 11 أعادت صوته المحفوظ الصحيح، دون نقل التصويت بين التواريخ.
- اختيرت ثلاث مراتب مختلفة، ثم استُخدم زر «تقديم مركز» وحُفظ الصوت بنجاح بالترتيب الجديد. لا اسم المصوّت بين الخيارات، ولا تكرار لاعب متاح في القوائم.
- نجحت 10 حالات لتصميم التصويت: العربية والإنجليزية × 320/360/390/430/1440 بكسل. عرض الصفحة مطابق لعرض الشاشة، دون قص عناوين المراكز؛ أزرار الترتيب بارتفاع 44 بكسل. عناصر الإدارة الظاهرة للحساب العادي صفر؛ Console بلا أخطاء أو تحذيرات في تبويب الاختبار.
- تمت مراجعة القواعد المنشورة المعلّمة بالنجمة في لوحة Firebase وحفظ نسخة احتياطية خارج ملفات النشر. أظهر جذر قاعدة الإنتاج مجموعتي `players` و`logs` فقط. لا كتابة بيانات إنتاج أو تغيير Auth.

## سجل مراجعة التفعيل قبل النشر — 8 سبتمبر 2026

استبدل المستخدم قرار نشر التحسينات العامة بطلب تفعيل كل الميزات، وسمح بكتابة بيانات اختبار محلية. نجح وقتها 113/113 اختبارًا نقيًا و27/27 اختبارًا لقواعد Firestore في المحاكي فقط. كان تعطيل الحسابات والتصويت مؤقتًا إلى حين نشر قواعد الإنتاج، وقد استُكمل ذلك كما في حالة النشر أعلاه.

- اختُبرت قواعد ربط الحساب وموافقته، حماية الإحصائيات، تعديل الملف، التوقيت الذري لأول مشاركة، ثبات الموعد عند إضافة المرشحين، منع الأصوات المكررة/الذاتية/المتأخرة، وخصوصية الأصوات الحية.
- كشف اختبار إضافي أن بعض رموز عزل اتجاه النص قد تتجاوز تصنيف Unicode في محرك القواعد؛ أُغلقت الحالة بمنعها صراحة ونجحت الاختبارات كلها بعد الإصلاح.
- من الواجهة المحلية: دخل المدير التجريبي، اعتمد طلب ربط، وسُجّلت أربع مشاركات جديدة. تأكدت القراءة المحلية من بقاء `openedAt` نفسه بعد الإضافات ومن إغلاقه بعد 86400000 مللي ثانية بالضبط.
- دخل الحساب الذي اعتمده المدير، وحفظ اسمًا ورقم قميص تجريبيين، وأرسل تصويتًا ثم غيّر ترتيبه. ظهر تأكيد الحفظ، وبقي عدد عناصر الإدارة المرئية له صفرًا، ولم تظهر أخطاء أو تحذيرات Console أثناء هذه التجربة.
- اختبارات القواعد وسكربت التجهيز مسحا/أعادا إنشاء بيانات QA في `demo-futbolista` فقط. إعداد Firebase وبيانات الإنتاج وAuthentication الإنتاج لم تتغير.
- إرسال بريد تحقق حقيقي، ودخول مدير الإنتاج، والتثبيت على iPhone فعلي ما زالت غير مختبرة. لا يُعد هذا التقرير إثباتًا لنشر القواعد أو الواجهة؛ يجب التحقق من ذلك في خطوة النشر.

المراجعات أدناه سجلات تاريخية؛ قيود عدم تشغيل المحاكي فيها كانت قبل موافقة 8 سبتمبر.

## آخر مراجعة: Community 500400 — 7 سبتمبر 2026

- 113/113 اختبار Node ناجح في الجولة الأخيرة؛ لا كتابة Firebase ولا اتصال شبكة أثناء الاختبارات. تشمل المحرك القديم، الإضافات الإحصائية، الثبات بعد تغيير الاسم، منع تكرار المرشحين، النقاط والتعادل، الحد الدقيق لـ24 ساعة، المسودات، جدولة الرسم، الروابط والكاش.
- فحص `node --check` لجميع ملفات JS وملفات اختبارات القواعد وسكربتات MJS ناجح، و`git diff --check` نظيف. فحص صياغة ملف اختبار القواعد لا يعني تشغيل قواعد Firestore.
- 26 حالة قواعد مجهزة للإصدار الحالي **لم تُشغّل** احترامًا لرفض كتابة بيانات حتى في المحاكي. لا اعتماد نهائي لأمن الميزات الجديدة قبل تنفيذ التحقق المناسب بتفويض منفصل.
- فُحصت Dashboard وLeaderboard وTable وPlayers وPlayer Profile والمقارنة المدمجة وHistory، بالعربية والإنجليزية، على 320 و360 و390 و430 و1440 بكسل. البيانات الحقيقية ظهرت: 26 مباراة و62 لاعبًا؛ فُحص أيضًا مصطفى (24 مشاركة و63 هدفًا) ومقارنته بصفوان. القراءة فقط، ولا ضغط حفظ/إضافة/تصويت.
- في مرور آلي من 70 حالة، كانت 3 مشاهدات مباشرة بعد تغيير المقاس على شاشة سابقة/مجاورة؛ أُعيد فحصها منفصلة بعد استقرار التخطيط والتأكد من الشاشة المطلوبة ونجحت. لم يُعامل عرض شاشة غير مطلوبة كنجاح.
- عرض المستند لم يتجاوز عرض الشاشة في الحالات المفحوصة؛ تمرير الجدول داخل حاويته. عناصر الإدارة الظاهرة للعامة: صفر. لا أسهم Leaderboard.
- سجل المتصفح في تبويب الفحص النظيف خالٍ من أخطاء JavaScript والتحذيرات. فُحص ظهور الفريقين والنتيجة في Safari على macOS بعد إزالة حركة شفافية التفاصيل، وحُفظت لقطة واضحة.
- في مراجعات محلية سابقة فُحصت واجهات الحساب المرتبط والطلب المعلق والمرفوض، وحقول التصويت ونتيجته المغلقة، ومسودة الاسم والاختيارات عند تغيير اللغة، وإظهار الإدارة بعد الدخول المحلي وإخفاؤها بعد الخروج. لم تُعد هذه الجولة تسجيل الدخول أو إرسال أي نموذج أو تجهيز المحاكي.
- التوقيت الجديد: أول سجل لمباراة جديدة ووثيقة التصويت يُحفظان في معاملة واحدة ووقت خادم؛ تنتهي المهلة بعد 24 ساعة دون تمديد بإضافة اللاعبين. لا فتح رجعي للمباريات التاريخية. أُزيل زر الفتح اليدوي وترجماته غير المستخدمة.
- تحديث الموارد 500400؛ إعداد Firebase و`data-engine.js` و`CNAME` لم تتغير. تصدير البيانات الخام كما هو. لا كتابة/حذف/ترحيل في الإنتاج ولا دفع GitHub أو نشر لهذه الدفعة.

### ما لم يمكن إثباته في هذه الجولة

- تطبيق قواعد Firestore الحالية وتنفيذ الحفظ الذري فعليًا: يتطلبان اختبارات كتابة محلية رفضها المستخدم.
- نجاح دخول إدارة الإنتاج، إرسال بريد تحقق/استعادة كلمة المرور، حساب جديد أو موافقة أو صوت فعلي: لم تُنفذ هذه العمليات.
- تطبيق PWA مثبت على iPhone حقيقي، لوحة مفاتيح iOS فعلية، وتعطيل الشبكة من الجهاز: لم يُختبر. اختبار المقاسات ليس بديلًا لجهاز حقيقي.
- لا نشر لقواعد الإنتاج ولا تفعيل للحسابات على الموقع؛ `COMMUNITY_ENABLED=false` حتى استكمال فحص الحماية وموافقة النشر.

التفاصيل، مسارات البيانات، خصوصية الأصوات بعد الإغلاق، وخطة الإطلاق في [COMMUNITY-RELEASE.md](./COMMUNITY-RELEASE.md).

---

## سجل مراجعة الإصدار السابق 500300

المحتوى التالي محفوظ كتقرير تاريخي؛ أرقامه وحدوده تخص ذلك الإصدار، وليست نتائج الجولة الحالية.

## البنية

- تطبيق صفحة واحدة متوافق مع GitHub Pages و`ftbll.live`، مبني بـHTML وCSS وJavaScript دون إطار عمل.
- `app.js` مسؤول عن Firebase Authentication والقراءة اللحظية من Firestore وحالة الواجهة والرسم والتنقل.
- `data-engine.js` محرك إحصائي نقي ومستقل يدعم السجلات الحالية والقديمة ويمنع تضخيم الإحصائيات بسبب الإدخالات المكررة.
- `i18n.js` يحتوي واجهتي العربية والإنجليزية وصيغ العد، و`ux-utils.js` يحتوي منطق UX النقي القابل للاختبار.
- `sw.js` و`manifest.json` وأيقونات PNG توفّر غلاف PWA قابلًا للتثبيت.

## التحسينات المنفذة

- واجهة رياضية داكنة ومتجاوبة بالكامل مع العربية وRTL وحالات التركيز وتقليل الحركة.
- لوحة رئيسية مركزة تعرض المباريات واللاعبين والفورم ولاعب الشهر والهدافين والسلاسل ونسبة الفوز.
- روابط قابلة للرجوع والمشاركة لكل قسم ولكل ملف لاعب، مع منع الروابط العامة من فتح شاشات الإدارة.
- روابط مشاركة نظيفة لملف اللاعب ولمقارنة لاعبين محددين، مع استرجاع طرفي المقارنة عند فتح الرابط مباشرة.
- فتح ملفات اللاعبين مباشرة من لوحة الصدارة والجدول والسجل والمقارنة وقائمة الزملاء.
- ملف لاعب محسّن يتضمن حصيلة فوز/تعادل/خسارة مرئية وزري مقارنة ومشاركة، ويرجع إلى القسم وموضع التمرير اللذين فُتح منهما.
- سجل مباريات قابل للبحث والتصفية والطي، ويعرض عشر مباريات في البداية ثم يحمّل المزيد عند الطلب لتقليل عناصر DOM على الجوال.
- فتح تفاصيل مباراة واحدة أو إغلاقها يحدّث عناصرها فقط ويحافظ على تركيز لوحة المفاتيح بدل إعادة رسم السجل كاملًا.
- إبراز عمود أو مقياس الترتيب النشط بصريًا ودلاليًا من دون إعادة أسهم Leaderboard.
- ترتيب مباريات اليوم نفسه حسب وقت إنشائها قبل حساب الفورم والسلسلة الحالية.
- تحقق أقوى من تاريخ المباراة وتوحيد مسافات وأحرف أسماء اللاعبين قبل فحص التكرار في الإدخالات الجديدة.
- منع حفظ نتيجة قبل اكتمال التحميل أو بلاعب لم يعد موجودًا، وإعادة حقلي الأهداف ونوع الهدف إلى قيم آمنة بعد الحفظ الناجح.
- تحسين تثبيت PWA على iPhone وAndroid ومراعاة المساحة الآمنة حول النتوء العلوي.
- تحميل اتجاه اللغة مبكرًا لمنع وميض LTR، وتحسين أسماء الوصول والعناوين وبنية المقارنة وعزل أسماء اللاعبين ثنائية الاتجاه.
- نوافذ آمنة ومتاحة لدخول الإدارة وإعادة الضبط، ورسائل تحميل ونجاح وخطأ واضحة.
- تحديث رقم موارد الواجهة إلى `500300` وإصلاح استراتيجية Service Worker لتخزين الاستجابات الناجحة فقط وانتظار الكتابة وحذف كاش Futbolista القديم فقط.

## سلامة البيانات والتوافق

- لم تتغير إعدادات Firebase أو Authentication أو أسماء مجموعتي `players` و`logs` أو أسماء الحقول.
- بقي `admin@ftbll.live` حساب الإدارة الوحيد، وبقيت Admin وEntry وSettings مخفية عن المستخدم العام.
- بقي تصدير JSON مبنيًا على بيانات Firestore الخام كما هو.
- لم تُنفذ أي كتابة أو حذف أو migration أو reset أثناء التطوير والاختبار.
- لم تُستخدم بيانات تجريبية بدل بيانات الإنتاج، وكانت مراجعة الإنتاج للقراءة فقط.
- راجعت القراءة الخام لبيانات الإنتاج: 62 لاعبًا و476 سجلًا. توجد 8 وثائق قديمة ناقصة لا تدخل في الحسابات، و89 سجلًا شرعيًا بصيغة `win` القديمة، ولا توجد تواريخ غير صالحة أو سجلات مرتبطة بلاعب محذوف أو أنواع مختلطة لبيانات المشاركة. توجد مجموعة تكرار واحدة من النوع نفسه يعالجها المحرك الحالي من دون تضخيم النتائج.
- لم تُضمّن قواعد Firestore في المستودع، لذلك لا يمكن إثبات صلاحيات الإنتاج من الكود المحلي وحده؛ لم تُغيَّر القواعد أو إعدادات Firebase.

## الاختبارات

- 41/41 اختبار Node ناجح لمحرك الإحصائيات ومنطق UX والترجمة والتنقل والمشاركة والتقسيم إلى صفحات والتحقق من مدخلات الإدارة ونسخة أصول PWA.
- فُحصت Dashboard وLeaderboard وTable وPlayers وPlayer Profile وCompare وHistory على عروض 320 و360 و390 و430 و1280 بكسل.
- لا يوجد تمرير أفقي للصفحة أو شريط الجوال؛ يظل تمرير الجدول داخليًا عند الحاجة.
- فُحصت العربية وRTL والإنجليزية، والروابط المباشرة، والرجوع، وروابط اللاعبين والمقارنة، وعناوين المتصفح، وتحميل المزيد في السجل.
- فُحصت دلالة ترتيب الجدول، وتمييز مقياس Leaderboard النشط، وعدم وجود أسهم Leaderboard، وحفظ تركيز لوحة المفاتيح عند طي تفاصيل المباراة.
- فُحص إخفاء عناصر الإدارة للعامة ونافذة الدخول وإغلاقها وإعادة التركيز، دون إرسال بيانات دخول.
- فُحصت وحدة التحكم في تبويب نظيف دون أخطاء JavaScript.
- لم يُختبر نجاح تسجيل الدخول الحقيقي لعدم توفر كلمة المرور، ولم يُضغط Add player أو Save entry أو زر التأكيد النهائي لإعادة الضبط، ولم يُجرَّب وضع انقطاع الشبكة فعليًا داخل المتصفح.
# Release 500403 — local MOTM and streak validation

Implemented locally; not published. See `RELEASE-500403.md` for the detailed
feature definitions, security boundaries, test results, and rules-first rollout.
173 unit/integration tests and 36 explicitly authorized local emulator security
tests passed. Responsive QA: 70 cases across Arabic/English and widths 320, 360,
390, 430, 1440. All production Firestore writes and deployments were avoided.
# 500404 — Voting standings and monthly history (2026-09-09)

Local/unpublished. All 225 tests passed (189 unit/integration + 36 emulator
security); 34 JS/MJS syntax checks and `git diff --check` passed. Ninety saved
AR/EN layout cases cover 320, 360, 390, 430 and 1440 px. Dashboard voting/MOTM
toggle, three award table sorts, profile totals/month history, direct profile
loading, admin login/logout, Compare and History checked. No final console
errors, page overflow, clipped new cards or public admin controls. No production
writes or deployment. See `RELEASE-500404.md` for calculation definitions,
limitations and the inherited rules-first rollout requirement.
