# Futbolista — Final UX Polish

## البنية الحالية

- تطبيق صفحة واحدة متوافق مع GitHub Pages و`ftbll.live`، مبني بـHTML وCSS وJavaScript دون إطار عمل.
- `app.js` مسؤول عن Firebase Authentication، والقراءة اللحظية من Firestore، وحالة الواجهة والرسم.
- `data-engine.js` محرك إحصائي نقي ومستقل يجمع السجلات المتوافقة تاريخيًا ويحسب النتائج والنماذج والترتيب.
- `i18n.js` يحتوي نصوص الواجهة الإنجليزية والعربية وصيغ العد.
- `ux-utils.js` يحتوي منطق UX النقي القابل للاختبار: البحث والفرز والمرشحات وfallback الشهر والمقارنة والتأكيد.
- `sw.js` و`manifest.json` وأيقونات PNG توفّر غلاف PWA قابلًا للتثبيت.

## المشاكل التي عولجت

- كان دخول الإدارة يعتمد على `prompt` وإعادة الضبط على `confirm`، دون حوار وصول متكامل أو حصر للتركيز.
- كانت صفحة لاعب الشهر فارغة عند غياب مباريات الشهر الحالي بدل الرجوع إلى آخر شهر متاح.
- لم تكن قوائم اللاعبين أو سجل المباريات قابلة للبحث والترشيح، وكان السجل يرسم كل التفاصيل مفتوحة.
- لم تحفظ العودة من ملف اللاعب موضع القائمة، ولم يكن الانتقال يضمن الرجوع لأعلى الصفحة.
- كانت أفضلية المقارنة تعتمد على اللون وحده، ولم يوجد زر تبديل.
- لم تكن صياغة المفرد والجمع متسقة، ولم توجد واجهة عربية أو RTL.
- روجعت الصياغة العربية شاشةً بشاشة لتكون مباشرة وطبيعية في سياق كرة القدم، واستُبدلت عبارات حرفية مثل «نبض الجولة» بـ«ملخص الفريق» مع تصحيح صيغ الأعداد العربية وتعريب نتائج ملف اللاعب وأسماء الفريقين.
- كانت أيقونة PWA بصيغة SVG فقط، ورقم الكاش قديمًا.
- كانت عملية إعادة الضبط الخطرة متاحة بعد تأكيد متصفح بسيط؛ أصبحت تتطلب دخول المدير وكتابة `RESET` حرفيًا.

## سلامة البيانات والتوافق

- لم تتغير إعدادات Firebase أو أسماء المجموعات `players` و`logs` أو أي اسم حقل.
- بقي `admin@ftbll.live` هو حساب الإدارة الوحيد.
- لم يتغير `data-engine.js` عن نسخة المصدر، بما في ذلك دعم السجلات القديمة ومنع تضخيم الإحصائيات بسبب الإدخالات المكررة.
- بقي تصدير JSON مبنيًا على بيانات Firestore الخام كما هو.
- لم تُنفذ أي كتابة أو حذف أو migration أو reset أثناء التطوير والاختبار.
- لم يُرفع أو يُنشر أي ملف.

## الاختبارات

- 23/23 اختبار Node ناجح: 15 اختبارًا للمحرك الحالي و8 اختبارات إضافية لمنطق UX.
- فُحصت Dashboard وLeaderboard وTable وPlayers وPlayer Profile وCompare وHistory على 320 و360 و390 و430 و1280 بكسل.
- لا يوجد تمرير أفقي للصفحة؛ تمرير الجدول وشريط التنقل داخلي فقط.
- فُحصت العربية وRTL، وبقاء أسماء اللاعبين كما هي، ووصول أول وآخر زر في شريط الجوال.
- فُحص فتح/إغلاق نافذة الدخول، Escape، حصر التركيز، إظهار كلمة المرور، ومسحها عند الإغلاق.
- فُحص إخفاء عناصر Admin وEntry وSettings للعامة (0 عناصر ظاهرة من أصل 7).
- فُحص تحميل نظيف للبيانات الحقيقية للقراءة فقط دون أخطاء أو تحذيرات Console.
- لم يُختبر نجاح تسجيل الدخول الحقيقي لعدم توفر كلمة مرور، ولم يُضغط Add player أو Save entry أو زر التأكيد النهائي لإعادة الضبط.
