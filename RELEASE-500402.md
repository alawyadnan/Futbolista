# Futbolista 500402 — جاهز للمراجعة، غير منشور

9 سبتمبر 2026. أساس العمل هو آخر نسخة GitHub المنشورة `a86868b`، وليس المتطلبات القديمة الملصقة. ما زالت الاستضافة GitHub Pages و`ftbll.live`؛ لا خدمات أو مكتبات جديدة.

## الجديد

- **كل المشاركات** من الملف الشخصي: سجل اللاعب كاملًا، إجماليات مرتبطة بفلتر التاريخ، والنتيجة وأهدافه وفريقه في كل مباراة. الرابط `#history/player/<encoded-playerId>` قابل لإعادة الفتح. مسح الفلاتر يرجع إلى السجل العام.
- **الحساب**: مراحل تأكيد البريد واختيار اللاعب وموافقة المدير، نص مفيد عند وصول الرسالة إلى Spam، زر إظهار/إخفاء كلمة المرور، ورسائل توضح الخطأ. الطلب المعلق يعرض اللاعب المطلوب؛ فتح التعديل وإلغاؤه لا يكتب بيانات.
- **المظهر**: نتيجة معنونة للفريقين باتجاه ثابت في العربية والإنجليزية، تباين أفضل للفريق الخاسر، أحجام نصوص أوضح، وبطاقة أرقام شخصية خضراء/صفراء/حمراء مع تسميات؛ اللون ليس الوسيلة الوحيدة لتمييز النتائج.

## السلامة والتوافق

- لم تتغير البيانات أو الحقول أو المجموعات أو إعدادات Firebase أو قواعد Firestore. محرك كرة القدم الأساسي وتصدير البيانات الخام لم يتغيرا.
- الحسابات الحالية والبريد وكلمة المرور ودخول `admin@ftbll.live` مستمرة. لم يُفعّل الهاتف أو SMS أو الفوترة. لم يبدأ مشروع تطبيق أصلي.
- هوية الحساب ما زالت Firebase UID، وربط الإحصائيات يعتمد على playerId؛ يمكن لتطبيق مستقبلي استخدام مشروع Firebase نفسه دون إنشاء قاعدة لاعبين بديلة.
- التصويت ما زال للمشاركين فقط، ثلاث مراتب مختلفة دون اختيار النفس، ولمدة 24 ساعة من أول حفظ لمباراة جديدة.
- ملفات النسخة الأصلية وZIP السابق محفوظة. لا نشر لهذه الدفعة حتى موافقة المستخدم.

## التحقق

141 اختبارًا دون شبكة/كتابة بيانات، و30 اختبار قواعد في محاكي Firebase المحلي فقط: **171 ناجحًا**. 101 حالة فحص متصفح باللغتين وعلى المقاسات الخمسة؛ تفاصيل النطاق والقيود في تقرير الجودة.

تشغيل الاختبارات العادية: `npm test`.

اختبارات القواعد تحتاج محاكي `demo-futbolista` المحلي وموافقة كتابة بيانات الاختبار؛ لا تُوجّه إلى مشروع الإنتاج.

لا بناء إلزامي للموقع؛ ملفات HTML/CSS/ES modules جاهزة لـGitHub Pages. عند الموافقة على النشر تُراجع تغييرات `main` أولًا، ثم تُنشر الملفات المختبرة معًا وتُراجع عملية Pages وإصدار الموارد 500402. لا حاجة لنشر قواعد Firebase لهذه الدفعة.
