# Futbolista — Community release 500401

## نطاق النشر الموافق عليه — 8 سبتمبر 2026

تعديل طلب المستخدم الأخير: **التصويت للمشاركين في التمرين نفسه فقط**، لا لكل حساب مرتبط. يُفرض تطابق هوية المصوّت مع قائمة مشاركة جلسة التاريخ المحددة في المحرك وقواعد Firestore، ويُستبعد أي صوت مستورد لغير مشارك من حساب النتائج. تظهر المراكز الأول والثاني والثالث (5/3/1) ببطاقات ذهبية/فضية/برونزية وزر تقديم مركز؛ لا اختيار للنفس أو تكرار لاعب. عند تزامن أكثر من جلسة مفتوحة، يختار اللاعب التاريخ صراحة، ويُفتح افتراضيًا أحدث تمرين شارك فيه. تبقى المهلة 24 ساعة من أول حفظ حتى لو كان تاريخ التمرين أقدم.

وافق المستخدم في آخر طلب على **نشر جميع الميزات، بما فيها الحسابات والتصويت**، ثم وافق صراحة على بيانات اختبار في المحاكي المحلي فقط. ألغى ذلك قرار نشر التحسينات العامة وحدها. تبقى بوابة `COMMUNITY_ENABLED=false` مؤقتًا إلى أن يُتحقق من نشر قواعد الحماية في الإنتاج؛ لا توجد migrations أو كتابة لبيانات الإنتاج أو تغيير لإعداد Authentication.

نتائج ما قبل التفعيل بعد شرط الحضور: 118 اختبارًا نقيًا و30 اختبار قواعد ناجحًا في `demo-futbolista`. نجحت تجربة الواجهة المحلية لاعتماد حساب لاعب، حفظ اسمه ورقمه، حفظ أول مشاركة وفتح التصويت، إضافة ثلاثة مشاركين دون تغيير وقت الفتح، وإرسال صوت وتعديل ترتيبه. في المراجعة الأخيرة نجح التصويت للتاريخ الذي حضره اللاعب، ومنعت الواجهة التصويت لتاريخ آخر لم يحضره. لا دخول أو صوت أو مشاركة تجريبية في الإنتاج.

أضيف اختبار مباشر لرموز Unicode المخفية؛ القواعد الآن ترفض رموز التحكم وعكس الاتجاه والعزل حتى عند تجاوز تحقق الواجهة. بعض جداول Unicode في محرك القواعد لا تشمل رموز العزل الأحدث، لذا تُمنع صراحة أيضًا.

## سجل التسليم المحلي قبل موافقة النشر — 7 سبتمبر 2026

الكود محلي وغير منشور. لا توجد migrations أو كتابة على Firestore الإنتاج، ولم تتغير إعدادات Firebase أو Authentication أو بيانات كرة القدم. `COMMUNITY_ENABLED = false` مقصود: الحسابات والتصويت لا يُفعّلان على الإنتاج قبل اعتماد قواعد الحماية واختبارها.

عند التسليم الأول نجح 113 اختبارًا دون كتابة بيانات، وتأجل تشغيل القواعد لغياب موافقة كتابة الاختبار حينها. أُنجزت لاحقًا الاختبارات المحلية الموضحة أعلاه بعد موافقة المستخدم. هذا السجل لا يعني أن قواعد الإنتاج نُشرت بالفعل.

## البنية الحالية

- تطبيق HTML/CSS وES modules مباشر، بلا بناء إلزامي، متوافق مع GitHub Pages و`ftbll.live`.
- `app.js`: التنقل والحالة واشتراكا `players` و`logs` ودخول الإدارة ورسم الإحصائيات.
- `data-engine.js`: مصدر الإحصائيات الموحّد؛ لم يتغير في هذه الدفعة. الهوية دائمًا `playerId`، والمباراة `matchId` إن وُجد وإلا التاريخ.
- `insights-engine.js`: أرقام الملف الشخصية، آخر 10 مشاركات، ومقارنة آخر 5 بالـ5 السابقة، اعتمادًا على المشاركات المطبّعة.
- `community-engine.js`: التحقق من الملف والتصويت، تخطيط فتح التصويت، وحساب النتائج؛ بلا اعتماد على Firebase.
- `community.js`: واجهات الحساب وربط الهوية والموافقة والتصويت، ومعاملات Firestore الجديدة. لا يملك اللاعب العادي مسارًا لتعديل الإحصائيات.
- `i18n.js` و`ux-utils.js`: العربية/الإنجليزية، RTL، الروابط، حفظ مسودات النماذج أثناء إعادة الرسم، وتجميع تحديثات الرسم.
- `sw.js`: يخزّن ملفات التطبيق المحلية فقط، لا استجابات Firebase أو الحسابات؛ الإصدار 500401.

## ما تغيّر

- حساب اختياري بالبريد وكلمة المرور، جلسة مستمرة، تأكيد البريد، استعادة كلمة المرور، خروج، وحالات طلب الربط.
- ربط حساب واحد بلاعب واحد بعد موافقة الإدارة، مع قفل متبادل في معاملة واحدة لمنع تضارب الموافقات.
- اسم عرض ورقم قميص 0–99 أو فارغ. التغييرات تجميلية منفصلة؛ لا تغيّر `players.name` الخام أو `playerId` أو السجلات.
- مقارنة مدمجة في ملف اللاعب: اختيار خصم واحد، ألوان متوازنة للطرفين، أشرطة مقارنة وروابط قابلة للمشاركة. حُذفت الشاشة المستقلة المكررة فقط؛ الروابط القديمة ما زالت تعمل.
- مخطط أهداف قابل لفتح تفاصيل المباراة، تطور الأداء، أفضل مباراة تهديفية، الهاتريك، نسبة المباريات التي سجّل فيها، وأطول سلسلة دون خسارة.
- تثبيت لاعب على الرئيسية محليًا على الجهاز؛ لا حساب ولا كتابة قاعدة بيانات لهذه الميزة.
- إعادة اتصال أكثر أمانًا، عدم تمكين حفظ/تصدير البيانات قبل اكتمالها، وعدم مزج لقطة جديدة بنصف بيانات قديمة.
- حفظ مسودة النموذج أثناء تبديل اللغة أو وصول تحديث؛ لا حفظ لكلمات المرور في التخزين المحلي.
- تحديث عرض موحّد عند وصول عدة لقطات، مع بديل زمني إذا عُلّق `requestAnimationFrame` في Safari. أُزيلت حركات شفافية المحتوى التي قد تخفي الصفحات أو تفاصيل المباريات.
- أخطاء الكاش لا تُخفي استجابة شبكة ناجحة؛ استرجاع الكاش عند 5xx أو انقطاع الشبكة لا يستبدل أخطاء 404.

## التصويت: 24 ساعة من أول حفظ

هذا السلوك يحل محل اقتراح «إنهاء التمرين وفتح التصويت» الموجود في المتطلبات الأقدم، وفق آخر طلب للمستخدم.

1. عند حفظ أول مشاركة لمباراة جديدة، يُحفظ سجل `logs` ووثيقة التصويت في **معاملة واحدة**: ينجح الاثنان أو لا يُحفظ أي منهما.
2. `openedAt = serverTimestamp()` وقت الحفظ على خادم Firestore، وليس تاريخ المباراة أو ساعة هاتف المدير.
3. الإغلاق دائمًا `openedAt + 24 hours`؛ لا حقل قابل للتعديل لتغيير المدة. قواعد الحماية تعتمد `request.time` وتمنع التصويت عند لحظة الإغلاق أو بعدها.
4. إضافة بقية المشاركين أثناء المهلة تضيف معرّفاتهم فقط؛ لا تمدّد الوقت ولا تعيد فتحه. الإدخالات العادية والعكسية للاعب نفسه لا تكرر المرشح.
5. العداد يبدأ من أول إدخال؛ اختيار ثلاثة لاعبين يصبح متاحًا بعد تسجيل أربعة مشاركين على الأقل، لأن التصويت للنفس ممنوع. التأخر في إكمال الإدخالات لا يمنح مهلة إضافية.
6. المباريات الموجودة تاريخيًا بلا وثيقة تصويت لا يُفتح لها تصويت بأثر رجعي عند تصحيح أو استكمال سجل قديم. بعد الإغلاق تبقى قائمة المرشحين ثابتة.
7. الهوية الحالية المعتمدة على التاريخ تعني جلسة واحدة لكل تاريخ. دعم `matchId` موجود في المحرك؛ لا تُغيَّر السجلات القديمة لإضافته.

العداد في الواجهة يستخدم ساعة الجهاز للعرض فقط. اختلافها لا يتجاوز قواعد الخادم، لكنه قد يعرض انتهاءً مبكرًا/متأخرًا؛ يظهر تنبيه وإعادة محاولة للنتائج إذا كان الوقت غير متوافق. حفظ الإدخال مع التصويت يحتاج اتصالًا صالحًا؛ لا نحفظ تصويتًا جزئيًا أو نؤجل وقت بدايته محليًا.

## البيانات الإضافية

لا تغيير لأسماء أو حقول `players` و`logs`. المسارات الجديدة فقط:

| المسار | الحقول | القراءة |
| --- | --- | --- |
| `users/{uid}` | `playerId`, `createdAt` | صاحب الحساب والإدارة |
| `playerClaims/{playerId}` | `uid`, `createdAt` | الإدارة |
| `accountRequests/{uid}` | `requestedPlayerId`, `email`, `status`, `createdAt`, `updatedAt` | صاحب الطلب والإدارة |
| `playerProfiles/{playerId}` | `displayName`, `preferredNumber`, `updatedAt` | عامة، بلا بريد أو UID |
| `sessionVotes/{encodedMatchKey}` | `matchKey`, `date`, `candidatePlayerIds`, `firstEntryId`, `openedAt` | عامة |
| `sessionVotes/{sessionId}/ballots/{uid}` | `voterPlayerId`, `firstPlayerId`, `secondPlayerId`, `thirdPlayerId`, `submittedAt`, `updatedAt` | صاحب الصوت أثناء المهلة؛ عامة بعد الإغلاق |

`firstEntryId` يشير للسجل الجديد الذي بدأ المهلة؛ قواعد الإنشاء تتحقق من وجوده في العملية نفسها ومن مطابقة التاريخ واللاعب. وثائق التصويت القديمة التي لا تحتويه تظل قابلة للقراءة وإضافة المرشحين خلال مهلة فتحها، ولا تُعدّل تلقائيًا.

لم تُنشأ هذه المجموعات في الإنتاج. أي بيانات QA موجودة من جولات سابقة محلية ومعزولة وليست بديلًا لبيانات الموقع.

## الصلاحيات والخصوصية

- يبقى `admin@ftbll.live` حساب الإدارة الحالي، بما فيه الجلسات القديمة. لا مزود Auth جديد؛ Email/Password ظهر مفعّلًا في مراجعة لوحة Firebase للقراءة فقط.
- الدخول وحده لا يسمح بادعاء هوية لاعب؛ تأكيد البريد وطلب الربط وموافقة الإدارة لازمة للتحرير والتصويت.
- `users` و`playerClaims` أقفال غير قابلة لإعادة الربط أو الحذف عبر التطبيق. استرداد حساب أو نقل هوية يحتاج إجراءً إداريًا منفصلًا ومدروسًا، وليس زرًا مخفيًا أو صلاحية للاعب.
- العامة واللاعبون لا يمكنهم كتابة `players` أو `logs` أو مسارات إحصائيات أخرى. المسارات غير المعروفة ممنوعة.
- الإدارة فقط تنشئ وثيقة التصويت وتضيف المرشحين أثناء المهلة. إزالة مرشح أو تغيير وقت الفتح أو الحذف ممنوع. الإدارة أصلًا موثوقة في بيانات المشاركة؛ القواعد لا تعيد بناء قائمة المشاركين كاملة من السجلات لكل تحديث.
- صوت واحد لكل UID ولكل جلسة، **من لاعب مشارك في الجلسة نفسها فقط**؛ ثلاث اختيارات مختلفة من المشاركين، دون التصويت للنفس. التعديل ممكن قبل الإغلاق فقط، ولا تُقبل نقاط رقمية من العميل. الحضور في تاريخ آخر لا يمنح حق التصويت هنا.
- النتائج لا تُجلب ولا تُعرض أثناء المهلة؛ حتى المدير لا يمكنه سرد الأصوات الحية عبر قواعد العميل.
- **الخصوصية بعد الإغلاق:** تعرض الواجهة المجموع النهائي؛ تسمح القواعد بقراءة أوراق التصويت القائمة على المعرّفات بعد الإغلاق، حتى يمكن حساب النتائج والتحقق منها في تطبيق ثابت دون خادم تجميع. لذلك يمكن لمن يقرأ البيانات ربط اختيار المصوّت بـ`voterPlayerId`، كما يظهر UID في معرّف الوثيقة، رغم عدم وجود بريد فيها. هذا ليس تصويتًا سريًا إلى الأبد. الواجهة توضّح ذلك قبل الإرسال. سرية الأوراق الدائمة تحتاج خدمة تجميع موثوقة منفصلة، وليست مضمونة بهذه النسخة.
- الترتيب النهائي: مجموع نقاط 5/3/1، ثم عدد المراكز الأولى فالثانية فالثالثة، ثم `playerId` تصاعديًا. الاسم التجميلي لا يغيّر النتيجة.

## الأداء والفهارس والنسخ الاحتياطي

- اشتراك واحد للملفات العامة وواحد لأحدث 10 جلسات. لا اشتراك لكل لاعب أو مرشح؛ جلسات أقدم ونتائج مغلقة تُقرأ عند فتح تفاصيلها وتُحفظ في ذاكرة الصفحة.
- للحساب اشتراكان خاصان؛ للإدارة استعلام الطلبات المعلقة؛ للصوت النشط اشتراك واحد بصوت المستخدم فقط.
- العداد يحدّث النص كل 15 ثانية مع مؤقت إغلاق منفصل، لا يعيد رسم الموقع كل ثانية.
- لا فهارس مركبة جديدة مطلوبة للاستعلامات الحالية؛ `firestore.indexes.json` فارغ. لا تنشره فوق إعدادات موجودة دون مراجعتها، ولا تحذف فهارس إنتاج.
- تصدير JSON القديم بقي `{exportedAt, players, logs}` من البيانات الخام فقط. لا تُضاف إليه الحسابات أو الأصوات خلسة. النسخ الاحتياطي لمجموعات المجتمع يحتاج خطة منفصلة قبل الاعتماد عليها فعليًا.
- حذف لاعب أو إعادة ضبط كرة القدم لا ينفّذ حذفًا متسلسلًا لوثائق الحساب أو الأصوات؛ لا يوجد cleanup أو migration تلقائي.

## الاختبارات الآمنة

تشغيل `npm test` أو `node --test test/*.test.js` لا يحتاج Firebase ولا شبكة ولا بيانات اعتماد. الاختبارات تستخدم كائنات في الذاكرة ومحاكاة طلبات Service Worker فقط.

اختبارات القواعد وسكربت التجهيز **تكتب وتمسح بيانات المحاكي المؤقتة**، ولذلك أضيف حاجز `FUTBOLISTA_ALLOW_EMULATOR_WRITES=1` فوق قيد `demo-futbolista` و`127.0.0.1`. سكربت التجهيز يعيد استخدام حسابات QA المحلية الأربع مع كلمات مرور عشوائية جديدة. هذا الحاجز ليس موافقة بحد ذاته؛ تشغيله في جولة 8 سبتمبر تم بعد إذن صريح، ولا يمنح أي إذن لكتابة الإنتاج.

## خطة تفعيل آمنة — لم تُنفّذ

1. إبقاء `COMMUNITY_ENABLED=false` حتى نشر القواعد. في مراجعة 8 سبتمبر فُتح سجل النسخ بنجاح وأظهر النسخة المنشورة المعلّمة بنجمة بتاريخ 18 فبراير 2026، 12:29 صباحًا: catch-all عامة القراءة، والكتابة للمدير. حُفظت نسخة خارج مجلد النشر. ظهر في قائمة مجموعات الإنتاج `players` و`logs` فقط؛ لا يجوز تفعيل المسارات الخاصة فوق القواعد القديمة.
2. اكتمل اختبار القواعد ومعاملة أول إدخال محليًا بعد التفويض الصريح؛ أي تغييرات لاحقة على القواعد تستلزم إعادة الاختبارات قبل النشر.
3. بعد موافقة نشر صريحة: حفظ نسخة من القواعد المنشورة والتحقق من جميع مجموعات المشروع الفعلية، ثم نشر القواعد المراجعة فقط. لا تغييرات Auth أو Firebase configuration ولا migrations أو حذف فهارس.
4. بعد تحقق الحماية: تفعيل بوابة المجتمع محليًا، تحديث إصدار الموارد معًا وإعادة الاختبارات، ثم دفع الإصدار إلى GitHub Pages بعد الموافقة. `CNAME` يبقى `ftbll.live`؛ لا حاجة لاستضافة Firebase.
5. فحص الإنتاج قراءةً فقط، ثم تجربة الحساب/الموافقة/أول إدخال/التصويت الفعلية عندما يصرّح المستخدم بإجراءاتها. لا يُستخدم حساب المدير أو بيانات المباراة الحقيقية كبيانات اختبار.

لا يُطلب من المستخدم تعديل الكود بنفسه. طلب التفعيل الكامل يشمل تجهيز نشر قواعد الحماية ثم الواجهة بهذا الترتيب؛ لا تغيير لمزودات Auth أو إعداد Firebase أو بيانات الإحصائيات.

## الملفات المعدلة والمضافة في الدفعة

معدلة: `app.js`, `i18n.js`, `index.html`, `manifest.json`, `package.json`, `styles.css`, `sw.js`, `ux-utils.js`, `test/static-assets.test.js`, `test/ux-utils.test.js`, `QA-REPORT.md`.

مضافة: `.gitignore`, `community-config.js`, `community-engine.js`, `community.js`, `insights-engine.js`, `personalization.js`, `firebase.json`, `firestore.rules`, `firestore.indexes.json`, `package-lock.json`, `COMMUNITY-RELEASE.md`, `rules-test/community.rules.test.js`, `scripts/seed-community-emulator.mjs`, `test/community-engine.test.js`, `test/community-integration.test.js`, `test/form-drafts.test.js`, `test/insights-engine.test.js`, `test/personalization.test.js`, `test/render-scheduler.test.js`, `test/service-worker.test.js`.
